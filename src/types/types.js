
// the types contain a string in this scenario just so they have information
// but the content is irrelevant for this example
export const types = {
    login: '[auth] login',
    logout: '[auth] logout'
}
