import React from 'react';
import ReactDOM from 'react-dom';
import { HeroesWebApp } from './HeroesWebApp';

ReactDOM.render(
  <React.StrictMode>
    <HeroesWebApp />
  </React.StrictMode>,
  document.getElementById('root')
);
